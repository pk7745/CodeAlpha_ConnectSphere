# ConnectSphere Client Environment Template
# When running locally, Vite proxy redirects /api and /socket.io to http://localhost:5000 automatically.
# For production hosting where the frontend is deployed separately (e.g. Vercel, Netlify):
VITE_API_URL=http://localhost:5000
